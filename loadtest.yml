config:
  target: "ws://localhost:8080/ws"
  phases:
    - duration: 600
      arrivalRate: 10     # clients/sec
      rampTo: 10
  engines:
    ws: {}
scenarios:
  - engine: "ws"
    flow:
      - think: 600          # stay connected for 60s
